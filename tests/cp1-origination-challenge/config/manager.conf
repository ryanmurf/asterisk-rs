[general]
enabled = yes
bindaddr = 0.0.0.0
port = 15038

; Least-privilege driver account for the harness. `write = system` is exactly
; what AMI Originate requires.
[cp1]
secret = cp1-local-only
read = all
write = system
