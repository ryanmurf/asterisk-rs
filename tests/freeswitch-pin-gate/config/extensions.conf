[pin-gate]
exten => 9000,1,Answer()
 same => n,Read(PIN,@PROMPT_DIR@/pin-prompt.wav,6,,1,5,2)
 same => n,GotoIf($["${PIN}" = "123456"]?10:20)
 same => 10,Verbose(0,PIN_GATE_RESULT=GRANTED)
 same => n,Playback(@PROMPT_DIR@/granted.wav)
 same => n,Hangup()
 same => 20,Verbose(0,PIN_GATE_RESULT=REJECTED)
 same => n,Playback(@PROMPT_DIR@/rejected.wav)
 same => n,Hangup()

exten => 9001,1,Answer()
 same => n,Dial(PJSIP/9199@@FS_CONTAINER_IP@:5060,1)
 same => n,Hangup()

exten => 9200,1,Answer()
 same => n,Dial(PJSIP/9201@@FS_CONTAINER_IP@:5060,20)
 same => n,Hangup()

exten => 9202,1,Answer()
 same => n,Dial(PJSIP/9203@@FS_CONTAINER_IP@:5060,10,S(3))
 same => n,Hangup()

exten => 9401,1,Answer()
 same => n,Wait(8)
 same => n,Hangup()
exten => 9402,1,Answer()
 same => n,Wait(45)
 same => n,Hangup()
exten => 9404,1,Answer()
 same => n,Dial(PJSIP/9304@@IMPAIRMENT_IP@:5060,1)
 same => n,Hangup()
exten => 9405,1,Answer()
 same => n,Wait(45)
 same => n,Hangup()

[outbound-proof]
exten => 9100,1,Wait(1)
 same => n,Playback(@PROMPT_DIR@/outbound-tone.wav)
 same => n,Wait(0.25)
 same => n,Hangup()

[m4-wait]
exten => s,1,Wait(5)
 same => n,Hangup()

[m4-timer-b-hold]
exten => s,1,Wait(70)
 same => n,Hangup()

[m4-concurrency]
exten => 0,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-0.wav)
 same => n,Hangup()
exten => 1,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-1.wav)
 same => n,Hangup()
exten => 2,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-2.wav)
 same => n,Hangup()
exten => 3,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-3.wav)
 same => n,Hangup()
exten => 4,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-4.wav)
 same => n,Hangup()
exten => 5,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-5.wav)
 same => n,Hangup()
exten => 6,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-6.wav)
 same => n,Hangup()
exten => 7,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-7.wav)
 same => n,Hangup()
exten => 8,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-8.wav)
 same => n,Hangup()
exten => 9,1,Wait(0.5)
 same => n,Playback(@PROMPT_DIR@/m4-tone-9.wav)
 same => n,Hangup()
